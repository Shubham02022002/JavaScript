"use strict"; // treats all JS code as newer version of JS.

// alert(4+3)  as of now we're using nodeJS, not the browser. Will see this later.

// While writing code, maintain code readability 

// DataTypes in JS

let name="xyz";
let age=10;
let isAdult=false;
let state;
let isMarried=null;

//number => 2^53
// bigInt => for numbers more than 2^53
// string => "some text"
// boolean => false/true
// null => standalone value
// undefined => 
// symbol => unique

//object -> Will discuss later

console.log(typeof null); // typeof null is object 
console.log(typeof undefined); // typeof undefined is undefined

